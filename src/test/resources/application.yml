server:
  port: 8077
  servlet:
    context-path: /e-library

spring:
  jpa:
    hibernate:
      ddl-auto: create
  #  show-sql: true
      generate-ddl: true
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  sql:
    init:
#      mode: always # применять файлы .sql
